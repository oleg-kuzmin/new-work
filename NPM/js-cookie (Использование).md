# [`js-cookie (Использование)`](./index.md)

```js
// Импорт библиотеки
import Cookies from 'js-cookie';

// Установка значения для cookies выполняется так
Cookies.set('foo', 'bar');

// А чтение так
const nameFromCookie = Cookies.get('name');
console.log(nameFromCookie);
```
