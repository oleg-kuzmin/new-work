# [`Явное указание типа`](../index.md)

В TS существует автоматическое неявное указание типа.

Но тип можно указать явно с помощью двоеточия.

Когда это однозначно необходимо:

- при создании переменной без значения.

## Пример

```ts
let str: string;
str = 'Hello world';
```
