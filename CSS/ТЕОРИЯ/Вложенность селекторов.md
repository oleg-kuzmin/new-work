# Вложенность селекторов

Используется оператор наследования '&'

```css
h2 {
  /* h2 span */
  & span {
    color: var(--color);
  }

  /* h2 + span */
  & + span {
    color: var(--color);
  }

  /* h2:has(span) */
  &:has(span) {
    color: var(--color);
  }
}
```
