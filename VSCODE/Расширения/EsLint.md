# [`EsLint`](../index.md)

## Настройка

```js
// .eslint.config.js

export default tseslint.config({
  rules: {
    // Запрет экспорта по умолчанию.
    'import/no-default-export': 'error',
  },
});
```
