# [`Параметры (неограниченное количество)`](../index.md)

Функции поддерживают rest и spread.

## Пример

```ts
function average(...nums: number[]) {
  const sum = nums.reduce((current, total) => current + total, 0);
  return sum / nums.length;
}
```
