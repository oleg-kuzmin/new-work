# [`Комбинация типов`](../index.md)

Знак `|` позволяет комбинировать несколько типов.

Необязательный параметр функции вернет такую комбинацию.

```ts
// string | undefined
function log(name: string, userId?: string): void {
  console.log('Hello', name);
}
```
