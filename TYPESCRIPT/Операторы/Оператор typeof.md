# [`Оператор typeof`](../index.md/#операторы)

Вернет тип переменной. Нельзя использовать вызовы функции как аргумент, только сами функции.

```ts
let str = 'Hello World';
type x = typeof str; // string
```
