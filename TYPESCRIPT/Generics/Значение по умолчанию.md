# [`Значение по умолчанию`](../index.md/#generics)

Указывается после знака `=`.

## Пример

```ts
interface AnyObject {
  [key: string]: unknown;
}

export async function request<T = AnyObject>(url: string): Promise<T> {
  const response = await fetch(url);
  return response.json();
}
```
