# [`Значение по умолчанию`](../index.md)

Указывается после знака равно.

## Пример

```ts
interface AnyObject {
  [key: string]: unknown;
}

export async function request<T = AnyObject>(url: string): Promise<T> {
  const response = await fetch(url);
  return response.json();
}
```
