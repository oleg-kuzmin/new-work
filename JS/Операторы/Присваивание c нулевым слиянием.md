# [`??= (Присваивание c нулевым слиянием)`](../index.md)

`variable &&= value`

- Если значение переменной `variable` - `null`/`undefined` присваивает переменной значение `value`
- Возвращает значение `value`.
