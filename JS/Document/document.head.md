# [`document.head`](../index.md)

Свойство позволяет получить объект страницы - `<head>`.

## Синтаксис

```js
document.head;
```

## Возвращает

### `element`

DOM-элемент `<head>`.

## Пример

```js
const head = document.head;
```
