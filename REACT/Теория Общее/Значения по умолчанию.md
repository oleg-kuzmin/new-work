# [`Значения по умолчанию`](../index.md)

Для того чтобы приложение не сломалось нужно передавать значения по умолчанию.

```jsx
// функция в props
const { searchMovies = Function.prototype } = props;
```